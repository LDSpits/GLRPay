<Window x:Class="GLRPay_OplaadStation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GLRPay_OplaadStation"
        mc:Ignorable="d"
        Title="GLRPay OplaadStation" Height="1080" Width="1920"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="9*"/>
        </Grid.RowDefinitions>

        <Rectangle Fill="#FF2DB406"/>

        <StackPanel >
            <TextBlock Text="GLRPay" Foreground="White" VerticalAlignment="Center" Margin="10,0,0,0" FontSize="70"/>
        </StackPanel>

        <Button HorizontalAlignment="Right"
                VerticalAlignment="Center"
                Background="Gray"
                BorderBrush="Transparent"
                Margin="0,0,10,0"
                Click="Button_Click">
            <TextBlock Text="X" FontSize="40" TextAlignment="Center" Margin="12,0" Foreground="WhiteSmoke"/>
        </Button>

        <Frame Grid.Row="1" x:Name="PageViewer" NavigationUIVisibility="Hidden">
            
        </Frame>
    </Grid>
    
</Window>
